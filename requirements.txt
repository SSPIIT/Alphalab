# Python dependencies for AlphaLab
#
# HOW THIS IS USED:
#   The Airflow container needs yfinance and pandas to run our DAG.
#   We'll reference this file in the Airflow service in docker-compose.yml
#   Week 3 onwards adds more libraries as we build more modules.

# Data ingestion
yfinance>=0.2.50       # 0.2.50+ has better rate-limit handling and Yahoo API fixes
pandas==2.2.2
pyarrow==16.0.0        # required for reading/writing Parquet files
requests>=2.32.0       # explicit pin — needed for session/User-Agent workaround
curl_cffi>=0.7.0
# Week 3 — uncomment when needed
# mlflow==2.13.0
# xgboost==2.0.3
# scikit-learn==1.4.2
# shap==0.45.0
# numpy==1.26.4